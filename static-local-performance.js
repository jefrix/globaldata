(function () {
  window.__globalDataLocalPerformanceInstalled = true;
  // Disabled: broad setInterval/requestAnimationFrame patching caused instability on some browsers.
  // Keep this file as a harmless cache-busted placeholder while targeted layer fixes remain active.
})();
